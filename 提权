linux提权

1、脏牛漏洞等操作系统内核漏洞，可以通过webshell等当前权限，上传扫描器（），也可以自行收集信息，寻找存在已有漏洞的服务

2、sudoer提权：
  将用户添加到有sudo权限的用户组

3、suid提权：
  原理：
    文件权限包括rwx，如果x位写的是s（rws），表示该文件在执行时，是以文件所有者身份执行
    linux中一切皆文件，cat，find等命令也是文件
    通过where cat，查询文件，修改文件权限即可（被加s权限的文件，会标红警告，提权完之后应该将权限改回去）
  利用方式：
find -prim -u=s
